// TODO: import Image
import { View, StyleSheet } from "react-native";
// TODO: import Logo from the logo.png in this directory;
// TODO(optional): You can also load the same image with this url: https://docs.expo.io/static/images/tutorial/logo.png

export function Step2() {
  return (
    <View>{/* TODO: Render the Logo that you imported with Image */}</View>
  );
}

// TODO: Give the image a height of 305, and a width of 159, then apply the style to the Image
const styles = StyleSheet.create({});
